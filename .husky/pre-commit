echo "🔍 Running pre-commit checks..."

echo "\n📝 Formatting code..."
pnpm format

echo "\n📊 Generating dependency graphs..."
pnpm deps:graph

echo "\n🔄 Checking for circular dependencies..."
pnpm circular:check || {
  echo "❌ Circular dependencies detected."
  exit 1
}

echo "\n🔗 Checking for high coupling..."
pnpm deps:check-coupling || {
  echo "⚠️  Warning: High coupling detected (see above)"
  # Don't exit - this is a warning only
}

echo "\n➕ Staging dependency graphs..."
git add docs/dependencies/*-deps.json

echo "\n🔎 Running linter..."
pnpm lint || {
  echo "❌ Linting failed. Fix the issues above."
  exit 1
}

echo "\n🔧 Running type checks..."
pnpm type-check || {
  echo "❌ Type check failed. Fix the type errors above."
  exit 1
}

echo "\n🧪 Running tests with coverage..."
pnpm test:coverage || {
  echo "❌ Tests failed. Fix the failing tests above."
  exit 1
}

echo "\n🏗️  Building project..."
pnpm build || {
  echo "❌ Build failed. Fix the build errors above."
  exit 1
}

echo "\n✅ All pre-commit checks passed!"
